# 任务：拓展抖音视频解析

## 背景

- 基于现有 `src/views/VideoParser.vue` 中 B 站视频解析实现，扩展抖音视频解析能力。
- 后端 `D:/JavaCodeStudy/wangyiyun-music` 已支持 `DOUYIN` 平台，前端继续沿用既有请求结构 `{ videoUrl, platform }`。
- 约束：保持最小修改范围，不影响现有 B 站视频解析功能；若需大幅调整，需先与用户确认。

## 执行方案

1. 审查 `VideoParser.vue` 中平台切换、输入提示、提交条件相关逻辑。
2. 放开抖音平台单选项，保持 YouTube 继续禁用。
3. 调整输入框提示文案，使其同时覆盖 B 站和抖音输入格式。
4. 保持 `handleParse` 请求结构不变，仅验证抖音平台值可透传到后端。
5. 完成后检查 B 站默认选择、抖音可选、YouTube 仍禁用，以及播放、下载、重置逻辑未受影响。

## 预期结果

- 页面可选择抖音平台并发起解析。
- B 站解析功能保持原样可用。
- 页面提示文案准确覆盖当前支持的平台输入形式。
